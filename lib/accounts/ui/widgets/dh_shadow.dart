import 'package:flutter/material.dart';

BoxShadow dhShadow() {
  return BoxShadow(
    color: Colors.black.withOpacity(0.25),
    spreadRadius: 0,
    blurRadius: 5,
    offset: Offset(2, 3),
  );
}
